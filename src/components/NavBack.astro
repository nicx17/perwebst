---
/**
 * NavBack provides a standardized back-navigation link.
 */
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a class="nav-back" href={href}>{label}</a>
